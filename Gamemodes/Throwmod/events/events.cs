function eventSchedule() { // 85 ms
    if($throwmodB >= 360) {
        
        $throwmodB = 0;
    }
    $throwmodB+=1;
    // schedule is running from throwmodSchedule(); and being called here
}
